# Task Workflow

Founder users can create tasks, assign owners, link clients/leads/projects, set priority, due date, approver, approval requirement, and internal notes.

Operations users can view assigned tasks, update allowed statuses, add comments/work notes, attach safe file metadata, and create Waiting on Stephen approval requests.

Every significant task mutation writes audit history. Task assignment also creates a notification for the assignee.
